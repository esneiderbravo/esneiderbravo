
# speclaw local code Compass (never commit)
.speclaw/

# speclaw managed-file refresh backups
*.bak

# speclaw workflow content (regenerated by init/update; never commit)
ai-specs/

# speclaw agent MCP config (per-developer; regenerated by init)
.mcp.json
